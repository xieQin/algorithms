const binarySearchTree  = (key) =>{
  let node = (key) => {
    this.key = key
    this.left = null
    this.right = null
  }

  let rootNode = null
}

function Node (value, left, right) {
  this.left = left
  this.right = right
  this.value = value
}